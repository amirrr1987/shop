# پلن فروشگاه (ایران)

نسخه: **1.4** · مخاطب: تیم فنی · وضعیت: پیش‌اجرا · scope: **تک‌فروشنده / سطح فروشگاه**

---

## ۱) خلاصه اجرایی

ساخت فروشگاه اینترنتی فارسی بر پایه قالب Rozer، با سه سطح:

| لایه | پوشه | تکنولوژی | نقش |
|------|------|----------|-----|
| فروشگاه | `web/` | Nuxt 4 + قالب FA | تجربه خرید مشتری |
| پنل | `panel/` | Vue 3 + Vite + Pinia | عملیات ادمین |
| API | `api/` | NestJS + TypeORM | منبع حقیقت قیمت، موجودی، سفارش |
| مشترک | `packages/shared/` | Zod | اسکیما و تایپ مشترک |
| تست API | `bruno/` | Bruno | سناریوهای دستی داخل گیت |

**هدف محصول:** مشتری بتواند محصول ببیند، سبد بسازد، با OTP وارد شود، پرداخت کند و سفارش را در حسابش ببیند؛ ادمین بتواند کاتالوگ و سفارش را بدون دست زدن به دیتابیس مدیریت کند.

**غیرهدف (فعلاً — قفل‌شده):**  
مارکت‌پلیس چندفروشنده · حسابداری کامل (سپیدار/هلو/…) · انبارداری حرفه‌ای (چند انبار، جابه‌جایی، بارکد پیشرفته، omnichannel) · اپ موبایل native · GraphQL  

**سطح محصول:** فقط **فروشگاه تک‌فروشنده** با موجودی ساده در حد فروش آنلاین (نه WMS / نه ERP).

---

## ۲) MVP در برابر قالب کامل

قالب Rozer صفحه زیاد دارد. برای جلوگیری از پراکندگی، محدوده MVP این است:

### داخل MVP
- Home (یک نسخه کافی است)
- Shop list + فیلتر پایه (دسته، قیمت، موجودی)
- Product detail + وریانت ساده
- Cart / Checkout / Thank-you
- Login OTP / My account (سفارش‌ها + آدرس)
- Panel: login ادمین، CRUD دسته/برند/محصول، لیست سفارش، تغییر وضعیت

### خارج از MVP (عقب‌افتاده)
- چند Home (`home-2..4`) و همه layoutهای `single-product-*`
- Compare
- Blog/CMS
- Wishlist (می‌تواند فاز ۲ دیرتر باشد)
- کوپن پیشرفته / باشگاه مشتریان / اعلان پوش

قانون: تا وقتی خرید E2E روی staging سبز نشده، صفحه جدید از قالب اضافه نمی‌شود.

---

## ۳) معماری و مرز مسئولیت‌ها

```text
[browser]
   │
   ├─ web (Nuxt)  ──REST──► api (NestJS) ──► Postgres
   │                           │
   └─ panel (Vue) ─REST/JWT───┤──► Redis (cache/queue)
                               └──► SMS / Payment / Storage
```

| لایه | اجازه دارد | اجازه ندارد |
|------|------------|-------------|
| `web` | نمایش، UX، SEO، state موقت UI | محاسبه نهایی قیمت، کم‌کردن موجودی، تأیید پرداخت |
| `panel` | CRUD عملیاتی، گزارش | دسترسی بدون Role، کلید درگاه/SMS |
| `api` | قوانین دامنه، تراکنش، یکپارچه‌سازی | UI |
| `shared` | Zod schema + types | منطق تجاری سنگین / دسترسی DB |

پروتکل: **REST + JSON** · مستند: **Swagger** · تست دستی: **Bruno** · Validation: **Zod**.

---

## ۴) تصمیم‌های فنی (قفل‌شده)

| موضوع | انتخاب | چرا | رد شد |
|-------|--------|-----|--------|
| ORM | **TypeORM** | ماژول رسمی Nest، Entity/Migration، مناسب سفارش/موجودی | Prisma (DX بهتر، یکپارچگی Nest کمتر) |
| Validation | **Zod + nestjs-zod** | اسکیمای مشترک monorepo، infer تایپ | class-validator |
| Workspace | **pnpm monorepo** | اشتراک `shared` بین سه اپ | سه ریپوی جدا بدون قرارداد |
| API Client | **Bruno** | فایل داخل گیت، بدون cloud اجباری | Postman Cloud |
| DB | **PostgreSQL** | رابطه‌ای، تراکنش، گزارش | MongoDB |
| صف/کش | **Redis + BullMQ** | OTP، SMS، callback، کش لیست | اجرای sync همه‌چیز |
| پول | **تومان (integer)** | بدون اعشار؛ جلوگیری از float | float / ذخیره مبهم ریال×۱۰ |

> Prisma فقط به‌عنوان گزینه DX سریع مطرح شد؛ برای این پروژه Nest + پنل عملیاتی، TypeORM انتخاب نهایی است.

---

## ۵) Monorepo هدف

```text
shop/
  package.json
  pnpm-workspace.yaml
  packages/
    shared/                 # zod schemas + inferred types
  api/                      # NestJS
  web/                      # Nuxt storefront
  panel/                    # Vue admin
  bruno/                    # API collection
  Plan.md
```

```yaml
# pnpm-workspace.yaml
packages:
  - 'api'
  - 'web'
  - 'panel'
  - 'packages/*'
```

`packages/shared` حداقل شامل:
- `auth.schema.ts` (otp request/verify)
- `product.schema.ts` / `catalog-filter.schema.ts`
- `cart.schema.ts` / `order.schema.ts`
- `common.ts` (pagination, money, phone IR, postal code)

### Bruno

```text
bruno/
  bruno.json
  environments/
    local.bru
    staging.bru
  health/
  auth/
  catalog/
  cart-order/
  admin/
```

قاعدة کیفیت: هر endpoint عمومی جدید بدون request Bruno در PR پذیرفته نمی‌شود.

---

## ۶) نگاشت قالب → دامنه → اولویت

| بخش قالب | ماژول API | اولویت |
|----------|-----------|--------|
| shop / product / modal | Catalog + Inventory + Media | MVP |
| cart / checkout / thank-you | Cart + Order + Payment + Shipping | MVP |
| login / my-account | Auth (OTP) + Users + Addresses | MVP |
| panel | Admin Auth + Catalog Admin + Orders Admin | MVP |
| wishlist | Wishlist | فاز ۲ |
| compare | Compare | بعداً |
| contact | Contact/Ticket | فاز ۳ |
| blog | CMS | بعداً |
| چند home / چند PDP layout | — | بعداً / حذف از scope |

---

## ۷) مدل دامنه (خلاصه Entity)

### هویت
- `User` (customer/admin)، `Role`، `OtpCode`، `Address`
- نقش‌های پنل: `admin` | `ops` | `support` (حداقل از فاز ۳؛ MVP می‌تواند `admin` alone باشد)

### کاتالوگ
- `Category`, `Brand`
- `Product`, `ProductVariant` (SKU)، `ProductImage`
- `Attribute` / `AttributeValue` (رنگ، ظرفیت، …)
- `InventoryMovement` (اختیاری ولی برای audit موجودی مفید است)

### خرید
- `Cart`, `CartItem` (guestId و/یا userId)
- `WishlistItem`
- `Coupon` (فاز ۲)
- `ShippingMethod`
- `Order`, `OrderItem`
- `Payment`, `Shipment`
- `Setting`, `Media`, `AdminAuditLog`

### پول و مالیات
- همه مبالغ: **عدد صحیح تومان**
- مالیات: نرخ از `Setting` (پیش‌فرض ۹٪) — تصمیم اعمال قبل/بعد تخفیف در بخش تصمیم‌های باز
- نمایش جلالی فقط در UI/فاکتور؛ در API تاریخ **ISO-8601**

---

## ۸) جریان‌های حیاتی

### ۸.۱ احراز هویت مشتری (OTP)
1. `POST /auth/otp/request` { phone } → محدودیت throttle
2. ارسال SMS از صف BullMQ
3. `POST /auth/otp/verify` → JWT (+ refresh اختیاری)
4. اگر سبد مهمان وجود دارد → **merge** به سبد کاربر

### ۸.۲ سبد و موجودی
- افزودن به سبد موجودی را قطعی کم نمی‌کند
- در شروع پرداخت: **رزرو کوتاه‌مدت** (مثلاً ۱۰–۱۵ دقیقه)
- انقضای رزرو با Schedule/Queue
- منبع حقیقت موجودی فقط API است

### ۸.۳ سفارش و پرداخت
وضعیت پیشنهادی سفارش:

`draft → awaiting_payment → paid → processing → shipped → completed`  
انشعاب: `cancelled` | `payment_failed` | `refunded` (فاز بعد)

قواعد پرداخت:
- ایجاد `Payment` با `authority/trackId` درگاه
- callback/verify باید **idempotent** باشد (پرداخت تکراری سفارش را دوبار paid نکند)
- COD مسیر جدا بدون درگاه؛ وضعیت `awaiting_cod_confirmation` یا معادل

### ۸.۴ اعلان‌ها
- بعد از پرداخت موفق / تغییر وضعیت ارسال → Job SMS (و در صورت نیاز ایمیل)
- شکست SMS نباید تراکنش سفارش را rollback کند؛ فقط retry در صف

---

## ۹) قواعد ایران (غیرقابل مذاکره در MVP)

1. واحد پول ذخیره‌سازی: **تومان integer**
2. ورود مشتری: **OTP موبایل**؛ رمز عبور مشتری اجباری نیست
3. اسرار درگاه/SMS فقط در env/secret manager — هرگز در گیت
4. موبایل و کدپستی با Zod (+ `@persian-tools/persian-tools` در سرویس)
5. استان/شهر از داده مرجع (نه فقط free text)
6. پشتیبانی COD اگر روش ارسال اجازه دهد
7. `web` منطق تجاری ندارد؛ `panel` بدون نقش مناسب به Admin API نمی‌رسد
8. لاگ audit برای تغییر وضعیت سفارش و تنظیمات حساس

---

## ۱۰) پکیج‌های NestJS (`api`)

### فاز ۰
```bash
pnpm add @nestjs/config @nestjs/swagger @nestjs/typeorm typeorm pg zod nestjs-zod helmet cookie-parser
pnpm add -D @types/cookie-parser
```

### فاز ۱
```bash
pnpm add @nestjs/jwt @nestjs/passport passport passport-jwt passport-local bcrypt @nestjs/throttler
pnpm add @nestjs/platform-express multer sharp
pnpm add @nestjs/bullmq bullmq ioredis @nestjs/cache-manager cache-manager
pnpm add @nestjs/event-emitter @nestjs/schedule
pnpm add -D @types/passport-jwt @types/passport-local @types/bcrypt @types/multer
```

### فاز ۲
```bash
pnpm add axios @persian-tools/persian-tools dayjs jalaliday
pnpm add nodemailer
pnpm add -D @types/nodemailer
# اختیاری S3-compatible: @aws-sdk/client-s3
```

### نصب نشود (فعلاً)
Prisma · class-validator/transformer · Mongo · GraphQL · Postman Cloud · UI kit سمت API

---

## ۱۱) فازبندی اجرا

### فاز ۰ — زیرساخت
هدف: قرارداد کار و اسکلت پایدار.

| کد | کار | Definition of Done |
|----|-----|-------------------|
| ۰.۰ | pnpm workspace + `packages/shared` | پکیج‌ها به هم لینک می‌شوند |
| ۰.۱ | TypeORM + Postgres + migration اولیه | `User`/`Setting` یا health entity migrate می‌شود |
| ۰.۲ | ConfigModule + `.env.example` | بدون secret در گیت |
| ۰.۳ | ساختار `modules/*` | قرارداد پوشه ثابت |
| ۰.۴ | Swagger + ZodValidationPipe | `/api/docs` زنده |
| ۰.۵ | CORS web/panel | prefight OK |
| ۰.۶ | Exception filter + response envelope | خطای یکشکل |
| ۰.۷ | Bruno `local` + `GET /health` | از Bruno 200 |

**خروجی فاز:** توسعه روی API بدون هرج‌ومرج شروع می‌شود.

---

### فاز ۱ — کاتالوگ و پنل پایه (MVP هسته)
هدف: داده استاتیک قالب با API جایگزین شود.

| کد | کار | اتصال قالب |
|----|-----|------------|
| ۱.۱ | Auth ادمین JWT + role | ورود panel |
| ۱.۲ | Category + Brand CRUD | منو/فیلتر |
| ۱.۳ | Product + Variant + Image | PDP / modal |
| ۱.۴ | Inventory پایه | add-to-cart واقعی |
| ۱.۵ | Upload امن تصویر (نوع/حجم/resize) | panel |
| ۱.۶ | Filter/Search API | shop/* |
| ۱.۷ | اتصال web (composables) | حذف mock |
| ۱.۸ | CRUD کاتالوگ در panel | عملیات روزانه |
| ۱.۹ | SEO پایه Nuxt برای محصول/دسته | title/meta |

**DoD:** لیست و جزئیات محصول از DB؛ ادمین بتواند محصول منتشر کند.

---

### فاز ۲ — خرید ایران
هدف: یک خرید واقعی end-to-end.

| کد | کار | اتصال قالب |
|----|-----|------------|
| ۲.۱ | OTP مشتری + throttle | login |
| ۲.۲ | Cart مهمان/کاربر + merge | cart |
| ۲.۳ | Checkout آدرس استاندارد | checkout |
| ۲.۴ | Shipping + COD | checkout |
| ۲.۵ | Order state machine | account/thank-you |
| ۲.۶ | درگاه (زرین‌پال/آیدی‌پی) + verify idempotent | thank-you |
| ۲.۷ | رزرو موجودی هنگام پرداخت | جلوگیری oversell |
| ۲.۸ | فاکتور/شماره سفارش + تومان | account |
| ۲.۹ | صف SMS بعد از رویدادها | اعلان |
| ۲.۱۰ | Wishlist ساده (اختیاری این فاز) | wishlist |

**DoD:** سفارش تست روی sandbox درگاه از سبد تا `paid` ثبت شود؛ موجودی درست کم شود.

---

### فاز ۳ — عملیات
هدف: اداره روزمره بدون DB.

| کد | کار |
|----|-----|
| ۳.۱ | داشبورد فروش |
| ۳.۲ | مدیریت سفارش + چاپ فاکتور |
| ۳.۳ | نقش‌های ops/support |
| ۳.۴ | Settings (ارسال، مالیات، درگاه، SMS) |
| ۳.۵ | Contact/Ticket |
| ۳.۶ | Audit log + بک‌آپ منظم |
| ۳.۷ | Coupon ساده |

**DoD:** ادمین چرخه سفارش را کامل از panel می‌بندد.

---

### فاز ۴ — مقیاس و پایداری
| کد | کار |
|----|-----|
| ۴.۱ | Redis cache لیست/دسته |
| ۴.۲ | ایندکس و پروفایل query |
| ۴.۳ | CDN تصاویر |
| ۴.۴ | Sentry/لاگ ساخت‌یافته + uptime |
| ۴.۵ | e2e خرید + e2e panel |
| ۴.۶ | bruno-cli در CI روی smoke tests |

---

## ۱۲) محیط‌ها و عملیات

| محیط | web | panel | api | DB |
|------|-----|-------|-----|----|
| local | :3000 | :5173 | :3001 | Docker Postgres/Redis |
| staging | subdomain | subdomain | subdomain | مدیریت‌شده |
| production | دامنه اصلی | `panel.` یا IP محدود | `api.` | backup روزانه |

حداقلی‌ها:
- SSL روی هر سه دامنه
- secret جدا per env
- migrate فقط forward در production
- محدودیت CORS دقیق در production
- rate limit سخت روی OTP

هاست پیشنهادی ایران: لیارا / ابرآروان / سرور اختصاصی — تصمیم نهایی باز است.

---

## ۱۳) کیفیت و Definition of Done عمومی

هر فیچر API وقتی Done است که:
1. Schema در `shared` (در صورت قرارداد عمومی)
2. تست/درخواست Bruno
3. Swagger به‌روز
4. خطاها در envelope استاندارد
5. برای پرداخت/موجودی: سناریوی شکست هم پوشش داده شده

حداقل تست‌ها تا پایان فاز ۲:
- OTP با provider mock
- پرداخت sandbox + replay callback
- یک خرید e2e happy path

---

## ۱۴) ریسک‌ها

| ریسک | اثر | کاهش |
|------|-----|------|
| oversell موجودی | نارضایتی / لغو | رزرو هنگام پرداخت + تراکنش DB |
| callback تکراری درگاه | سفارش/پرداخت دوبل | idempotency key / unique authority |
| هزینه SMS و سوءاستفاده OTP | هزینه / بلوکه شدن | throttle + سقف روزانه شماره |
| بزرگ شدن زودهنگام قالب | تأخیر MVP | قفل scope بخش ۲ |
| پراکندگی تایپ بین اپ‌ها | باگ قرارداد API | monorepo shared + Zod |
| وابستگی تک‌درگاه/تک‌SMS | قطع سرویس | abstraction + قابلیت سوییچ از Setting |

---

## ۱۵) تصمیم‌های باز (باید قبل/حین فاز مربوطه بسته شوند)

| # | موضوع | گزینه‌ها | مهلت پیشنهادی |
|---|--------|----------|----------------|
| D1 | درگاه اول | زرین‌پال / آیدی‌پی / … | قبل ۲.۶ |
| D2 | SMS اول | کاوه‌نگار / ملی‌پیامک / … | قبل ۲.۱ |
| D3 | مالیات روی قیمت نمایشی | شامل / جدا | قبل ۲.۵ |
| D4 | Guest checkout بدون OTP؟ | بله محدود / خیر اجباری OTP | قبل ۲.۲ |
| D5 | محل فایل‌ها | دیسک سرور / S3-compatible | قبل ۱.۵ |
| D6 | دامنه panel | زیر‌دامنه / VPN-only | قبل staging |
| D7 | Wishlist در فاز ۲ یا ۳ | ۲ / ۳ | پایان فاز ۱ |

---

## ۱۶) وضعیت فعلی و قدم بعدی

- [x] قالب فارسی در `web`
- [x] اسکلت `panel`
- [x] اسکلت `api` (Nest خام)
- [ ] فاز ۰: monorepo + TypeORM + Zod + Swagger + Bruno
- [ ] فاز ۱: کاتالوگ + اتصال web/panel
- [ ] فاز ۲: خرید E2E ایران

**قدم بعدی فوری (۰.۰ → ۰.۷):**  
ساخت workspace و `packages/shared` → TypeORM/Postgres در `api` → health + Swagger → اسکلت `bruno/`.

تا قبل از سبز شدن فاز ۰، فیچر دامنه (محصول/سفارش) شروع نشود.

---

## ۱۷) شکاف‌ها و بک‌لاگ آگاهانه

مواردی که در فازبندی اصلی کم‌رنگ بودند و نباید فراموش شوند. اولویت‌ها نسبت به MVP خرید تنظیم شده‌اند.

### ۱۷.۱ باید قبل از فروش واقعی بسته شود (همراه MVP / انتهای فاز ۲)

| کد | موضوع | چرا | پیشنهاد جاگذاری |
|----|--------|-----|-------------------|
| G1 | جستجوی متنی محصول/SKU | فیلتر سایدبار جای سرچ را نمی‌گیرد | فاز ۱.۶+ یا ۱.۱۰ |
| G2 | بازیابی پرداخت ناموفق / ادامه پرداخت | کاربر از درگاه ناموفق برمی‌گردد | فاز ۲.۶ |
| G3 | صفحات حقوقی و اعتماد | قوانین، حریم خصوصی، مرجوعی، درباره ما | استاتیک در فاز ۱؛ قابل ویرایش در فاز ۳ |
| G4 | پیگیری ارسال برای مشتری | کد رهگیری + SMS «ارسال شد» | فاز ۲.۵ / ۲.۹ |
| G5 | خطای checkout و سفارش نیمه‌کاره | فقط happy path کافی نیست | فاز ۲.۲–۲.۶ |
| G6 | جزئیات قیمت ایران | قیمت قبلی، تخفیف‌خورده، تومان یکدست، وریانت | فاز ۱.۳ |
| G7 | داده مرجع استان/شهر + timezone `Asia/Tehran` | checkout و گزارش | فاز ۰.۲ / ۲.۳ |
| G8 | شماره‌گذاری فاکتور (سریال/پیشوند) | پشتیبانی و حسابداری ساده | فاز ۲.۸ |
| G9 | Sandbox درگاه و SMS از روز اول | بدونش E2E واقعی ممکن نیست | قبل ۲.۱ و ۲.۶ |

### ۱۷.۲ فاز ۲–۳ (عملیات و اعتماد)

| کد | موضوع | پیشنهاد جاگذاری |
|----|--------|-------------------|
| G10 | مرجوعی / لغو بعد از پرداخت + برگشت موجودی (+ refund درگاه در صورت نیاز) | فاز ۳ یا ۲.۷+ |
| G11 | مدیریت بنر/اسلایدر/بخش‌های Home از panel | فاز ۳ (محتوا) |
| G12 | نظر و امتیاز محصول (با تأیید ادمین) | فاز ۳ |
| G13 | اعلان داخل حساب کاربری (علاوه بر SMS) | فاز ۳ |
| G14 | خروجی اکسل سفارش / چاپ لیبل انبار | فاز ۳.۲ |
| G15 | رسید ورود کالا به انبار (نه فقط عدد موجودی) | فاز ۳ |
| G16 | اطلاع‌رسانی موجود شدن کالا | فاز ۳ |
| G17 | سقف تعداد در سبد / حداقل مبلغ سفارش | فاز ۲.۲ |
| G18 | اینباکس تماس (`contact`) در panel | فاز ۳.۵ |
| G19 | نقش چند ادمین و قفل ویرایش سفارش | فاز ۳.۳ |
| G20 | بک‌آپ/بازیابی به‌عنوان روال مستند | فاز ۳.۶ |

### ۱۷.۳ بعد از پایدار شدن فروش (رشد)

| کد | موضوع | نکته |
|----|--------|------|
| G21 | Analytics / event خرید | ماتومو یا معادل؛ حداقل thank-you event |
| G22 | SEO کامل | sitemap، canonical، JSON-LD محصول، بهینه‌سازی تصویر |
| G23 | کوپن تک‌بار / اولین خرید | بعد از Coupon ساده |
| G24 | PWA / پوش مرورگر | اختیاری |
| G25 | باشگاه مشتریان / کیف پول | بعد از MVP قطعی |
| G26 | Meilisearch/Elastic برای سرچ بزرگ | فقط اگر حجم کاتالوگ بالا رفت |

### ۱۷.۴ آگاهانه خارج از scope (درست که عقب است)

- مارکت‌پلیس چندفروشنده
- حسابداری کامل (سپیدار/هلو)
- چند انبار omnichannel
- اپ native
- GraphQL
- Compare و چند Home/PDP layout قالب

### ۱۷.۵ تصمیم‌های باز اضافه‌شده

| # | موضوع | گزینه‌ها | مهلت |
|---|--------|----------|------|
| D8 | موتور سرچ اولیه | Postgres `ILIKE` / Meilisearch | قبل G1 |
| D9 | مرجوعی در MVP؟ | فقط لغو قبل ارسال / مرجوع کامل | قبل G10 |
| D10 | محتوای Home | ثابت در کد تا فاز ۳ / CMS زودهنگام | پایان فاز ۱ |
| D11 | Analytics | هیچ / ماتومو / GA | قبل staging عمومی |

**قانون:** آیتم‌های ۱۷.۱ بدون تیک، فروش عمومی (production traffic) توصیه نمی‌شود — حتی اگر خرید E2E فنی سبز شده باشد.

---

## ۱۸) آمادگی go-live و موارد حقوقی / عملیاتی / مالی

این بخش مکمل بک‌لاگ فیچر (۱۷) است: چیزهایی که فروشگاه را «از نظر کسب‌وکار و عملیات» قابل اعتماد می‌کند.

### ۱۸.۱ پیش‌نیازهای اعتماد و قانونی ایران

| کد | موضوع | وضعیت در پلن | اقدام |
|----|--------|--------------|--------|
| L1 | اینماد / ساماندهی (در صورت نیاز مدل کسب‌وکار) | غایب | قبل از درگاه واقعی پیگیری شود |
| L2 | قرارداد و مدارک درگاه بانکی | فقط sandbox در G9 | چک‌لیست مدارک جدا نگه داشته شود |
| L3 | صفحات قوانین، حریم خصوصی، مرجوعی | G3 | قبل از ترافیک عمومی منتشر |
| L4 | سامانه مودیان / صورتحساب الکترونیکی | غایب | اگر مشمولید: فاز جدا بعد از MVP؛ مدل Order از الان فیلدهای لازم را باز بگذارد |
| L5 | مالکیت/مجوز تصاویر و محتوای محصول | غایب | فرایند محتوا در panel (منبع تصویر) |
| L6 | SLA پشتیبانی (ساعت پاسخ، روزهای کاری) | غایب | متن در صفحه تماس + تعهد داخلی تیم |

### ۱۸.۲ مالی و مغایرت‌گیری

| کد | موضوع | پیشنهاد |
|----|--------|----------|
| F1 | کارمزد درگاه در گزارش سود | فیلد/گزارش فاز ۳.۱ |
| F2 | مغایرت‌گیری روزانه پرداخت‌ها با پنل درگاه | job یا چک‌لیست ops روزانه بعد از فاز ۲ |
| F3 | هزینه ارسال رایگان با سقف مبلغ سبد | قانون در Shipping/Setting — فاز ۲.۴ |
| F4 | تصمیم کیف پول/اعتبار فروشگاهی | فعلاً خارج MVP؛ اگر بعداً آمد در Order/Payment جدا مدل شود (D12) |

### ۱۸.۳ موجودی و کاتالوگ عمیق‌تر

| کد | موضوع | پیشنهاد |
|----|--------|----------|
| I1 | منع موجودی منفی در DB (constraint/تراکنش) | همراه ۱.۴ و ۲.۷ |
| I2 | محصول پک / ترکیبی | بعد از MVP مگر مدل کسب‌وکار اجبار کند |
| I3 | SKU داخلی vs کد تأمین‌کننده | فیلد روی Variant از فاز ۱.۳ |
| I4 | جدول مشخصات فنی (نه فقط HTML توضیح) | Attribute از قبل هست؛ UI جدول در PDP فاز ۱–۲ |
| I5 | ویدیو محصول / خریداری‌شده با هم / صفحه برند | رشد — بعد از پایدار شدن فروش |

### ۱۸.۴ امنیت و ضدسوءاستفاده

| کد | موضوع | پیشنهاد |
|----|--------|----------|
| S1 | ضدبات و سقف OTP (شماره/IP/روزانه) | همراه ۲.۱ و Throttler |
| S2 | محدودیت نرخ روی پرداخت و callback | فاز ۲.۶ |
| S3 | سیاست کوکی/JWT برای web (XSS/CSRF) | فاز ۱.۷ هنگام اتصال auth |
| S4 | حداقل «حذف/غیرفعال‌سازی حساب» و پاکسازی داده | فاز ۳+ |
| S5 | عدم لاگ کردن کد OTP و توکن خام | از روز اول در سرویس SMS/Auth |

### ۱۸.۵ فنی go-live (تصمیم + اجرا)

| کد | موضوع | گزینه‌ها / اقدام |
|----|--------|------------------|
| T1 | استراتژی Nuxt SSR/ISR برای محصول و دسته | تصمیم قبل ۱.۹ |
| T2 | نسخه‌گذاری API (`/v1`) | از فاز ۰ در prefix |
| T3 | Feature flag درگاه/SMS واقعی | Setting یا env قبل staging→prod |
| T4 | سیاست migration روی production | فقط forward؛ مسئول مشخص |
| T5 | Observability: خطا، صف، نرخ شکست SMS/پرداخت | فاز ۴.۴ زودتر روی staging |
| T6 | Seed/داده دمو برای توسعه و دمو مشتری | فاز ۰ پایان / شروع ۱ |
| T7 | Timezone پیش‌فرض سرور و گزارش‌ها `Asia/Tehran` | G7 |

### ۱۸.۶ چک‌لیست go-live (قبل از باز کردن به مشتری واقعی)

- [ ] دامنه + SSL برای `web` / `panel` / `api`
- [ ] `.env` production بدون secret در گیت؛ بک‌آپ Postgres زمان‌بندی‌شده و تست restore
- [ ] درگاه **واقعی** با verify و تست مبلغ کم
- [ ] SMS **واقعی** با قالب تأییدشده و سقف ضدسوءاستفاده
- [ ] صفحات حقوقی (L3) و اطلاعات تماس معتبر
- [ ] G1–G9 از بخش ۱۷ تیک خورده
- [ ] سناریوی پرداخت ناموفق و «ادامه پرداخت» تست شده
- [ ] نقش ادمین محدود؛ panel روی زیر‌دامنه یا دسترسی محدود
- [ ] مانیتورینگ حداقل: down api + خطای پرداخت
- [ ] یک نفر owner on-call برای روز اول فروش

### ۱۸.۷ مالکیت و زمان‌بندی سبک

| حوزه | Owner پیشنهادی (جایگزین با نام واقعی) | وابسته به |
|------|----------------------------------------|-----------|
| فاز ۰ زیرساخت | Backend | — |
| کاتالوگ + panel | Backend + Panel | فاز ۰ |
| OTP/پرداخت | Backend | قرارداد درگاه/SMS |
| اتصال قالب web | Frontend | فاز ۱ API |
| حقوقی/اینماد/مدارک | کسب‌وکار | قبل درگاه واقعی |
| محتوا/عکس محصول | محتوا / ops | قبل فروش |

تقویم تقریبی (قابل تغییر؛ فقط برای جلوگیری از فاز بی‌زمان):

| فاز | برآورد خام |
|-----|------------|
| ۰ | ۱ هفته |
| ۱ | ۲–۳ هفته |
| ۲ | ۲–۴ هفته |
| ۳ | ۲–۳ هفته |
| ۴ | پیوسته |

### ۱۸.۸ تصمیم‌های باز این بخش

| # | موضوع | گزینه‌ها | مهلت |
|---|--------|----------|------|
| D12 | کیف پول فروشگاهی | نه در ۱۲ ماه اول / بله بعد فاز ۳ | پایان فاز ۲ |
| D13 | مشمولیت سامانه مودیان | بله (فاز جدا) / خیر فعلاً | قبل فروش وسیع |
| D14 | SSR/ISR Nuxt | SSR همه PDP / ISR کش‌شده | قبل ۱.۹ |
| D15 | دسترسی panel | اینترنت + 2FA / فقط VPN | قبل staging عمومی |

**قانون go-live:** بدون تیک بخش ۱۸.۶، سوییچ DNS عمومی یا کمپین تبلیغاتی انجام نشود — حتی اگر محیط technical production بالا باشد.
